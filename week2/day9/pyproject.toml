[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "advent-agent9"
version = "0.1.0"
dependencies = ["requests"]

[project.scripts]
agent9 = "agent9:main"

[tool.setuptools]
py-modules = ["agent9"]
