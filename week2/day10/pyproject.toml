[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "advent-agent10"
version = "0.1.0"
dependencies = ["requests"]

[project.scripts]
agent10 = "agent10:main"

[tool.setuptools]
py-modules = ["agent10"]
