# CLAUDE.md — AI Advent Challenge #8

> Этот файл — постоянный контекст проекта. Claude Code читает его автоматически при запуске
> из папки `C:\ADVENT`. Здесь хранится общий контекст челленджа, выбранные инструменты,
> ограничения для агента и журнал работы по дням. Журнал по дням дополняется по мере
> выполнения заданий — см. раздел «Журнал по дням» внизу.

---

## 1. Что это за проект

Участие в **AI Advent Challenge #8** — 7-недельном учебном марафоне по работе с LLM и
ИИ-агентами (организатор — курс mobiledeveloper.tech). Цель проекта: выполнять ежедневные
задания челленджа, оформлять результат и сдавать его в срок.

- Сайт челленджа: https://mobiledeveloper.tech/ai_advent_8
- Список сервисов (Notion): https://mobile-developer.notion.site/AI-Advent-3298aa43ca5580819b7ae6f5c90ee6f7
- Таблица для сдачи: https://docs.google.com/spreadsheets/d/1GknoR-rJjllUj7WvgXFw-KxJJWwQOP9pYYji5Mx6uyM/edit?gid=0#gid=0

## 2. Правила челленджа (важно соблюдать)

- Задания выдаются **по будням в 14:00 по Москве**.
- Нужно выполнить **5 заданий за неделю**. Кто не сдал все 5 заданий предыдущей недели
  к понедельнику 14:00 МСК — **выбывает**.
- Сдача: заполнить свой слот в Google-таблице ссылками (режим комментирования).
  Организатор аппрувит. Нет результата — удаляют из таблицы/чата.
- **Формат сдачи каждого задания: Видео + Код.** (Видео участник записывает сам.)
- Проверка заданий — выборочная и асинхронная. Могут попросить переделать.
- Общение в чате: вежливо, без мата и оскорблений, без оффтопа.

## 3. Инструменты (стек проекта)

| Роль | Выбор | Детали |
|------|-------|--------|
| Harness (агент-помощник) | **Claude Code** | Сейчас в Desktop, переходим в терминал. Работа из папки `C:\ADVENT`. |
| LLM API (для заданий) | **ProxyAPI.ru** | Российский посредник, оплата рублями с карты РФ. Один ключ → DeepSeek / OpenAI / Gemini / Anthropic. OpenAI-совместимый формат. |
| Язык/среда | **Python** | Минимальный код для REST-запросов. Модель `gpt-4o-mini` через OpenAI-эндпоинт ProxyAPI. |
| VPS/хостинг | (пока не требуется) | Понадобится позже по программе. Варианты: timeweb.com, beget.com/ru/vps, hosting-vds.com. |

### Harness ≠ API (не путать)
- **Claude Code (harness)** — инструмент, который помогает *писать и запускать код*. Работает на подписке Claude.
- **ProxyAPI/DeepSeek (API из задания)** — облачная LLM, к которой *обращается написанный код* по REST. Именно это требуют задания («облачная модель, доступная через REST»).

### Доступ к ProxyAPI
- Регистрация и ключ: https://console.proxyapi.ru (нужен только email, без иностранного телефона).
- Оплата: рублями с карты любого банка РФ.
- Авторизация во всех запросах: заголовок `Authorization: Bearer <КЛЮЧ>`.
- Базовый адрес: `https://api.proxyapi.ru`, дальше идентификатор провайдера:
  - OpenAI (подтверждено докой): `https://api.proxyapi.ru/openai/v1` → `/chat/completions`
  - DeepSeek: в ProxyAPI **недоступен** (нет в консоли/доке на 2026-06). Не используем.

### Решение: провайдер и модель (зафиксировано 2026-06-03)
- Провайдер: **OpenAI** через ProxyAPI (OpenAI-совместимый формат).
- Эндпоинт: `https://api.proxyapi.ru/openai/v1/chat/completions`.
- Модель: **`gpt-4o-mini`** (подтверждена в списке моделей ProxyAPI: https://proxyapi.ru/docs/openai-models).
- Причина выбора: задания челленджа минимальные → дёшево + быстро + качества хватает, без оверкилла.
- Ключ хранить в переменной окружения (`PROXYAPI_KEY`), НЕ коммитить в код/git.

---

## 4. ОГРАНИЧЕНИЯ ДЛЯ АГЕНТА (строго соблюдать)

Эти правила обязательны при работе над любым заданием челленджа:

1. **Делать ровно то, что требует задание дня — не больше.** Не добавлять фич, абстракций,
   «улучшений», фреймворков, конфигов, тестов, которые задание не просит.
2. **Не выдумывать.** Если задание чего-то не уточняет — спросить пользователя, а не
   достраивать предположения. Не изобретать несуществующие требования.
3. **Минимализм кода.** Задания первых дней решаются в десятки строк. Держать решение
   максимально простым и читаемым. Никакого оверинжиниринга.
4. **Точность фактов про API.** Не выдумывать эндпоинты, имена моделей, параметры. Если
   не уверен — проверить в актуальной документации или спросить, а не угадывать.
5. **Не выходить за рамки текущего дня.** Не начинать задания будущих дней заранее без
   явной просьбы пользователя.
6. **Соответствие формату сдачи.** Помнить, что результат = Видео + Код. Код должен быть
   таким, чтобы пользователь мог его легко показать и объяснить на видео.
7. **Безопасность.** API-ключи только в переменных окружения, не в коде и не в репозитории.
8. **Сверяться с формулировкой.** Перед сдачей сверять результат с дословной формулировкой
   задания (см. файл `первые2днячелленджа.txt` и последующие).
9. **Журналировать.** После выполнения задания дня дополнять раздел «Журнал по дням» ниже:
   что сделано, какие файлы созданы, ссылка на код, статус сдачи.

---

## 5. Задания (формулировки)

Полные тексты заданий первых двух дней — в файле `первые2днячелленджа.txt`.

### День 1. Первый запрос к LLM через API
Написать минимальный код, который:
- отправляет запрос в LLM через API;
- получает ответ;
- выводит его в консоль или простой интерфейс (CLI / Web).
**Результат:** код, отправляющий запрос в LLM через API и получающий ответ.
**Формат:** Видео + Код.

### День 2. Формат ответа
Отправить тот же запрос, но:
- добавить явное описание формата ответа;
- добавить ограничение на длину ответа;
- добавить условие завершения ответа (stop sequence или явная инструкция).
Сравнить ответы: без ограничений vs с ограничениями.
**Результат:** один и тот же запрос с разным уровнем контроля ответа через API.
**Формат:** Видео + Код.

### День 3. Разные способы рассуждения
Взять одну задачу (логическую/алгоритмическую/аналитическую) и решить её через API
четырьмя способами:
- прямой ответ без доп. инструкций;
- инструкция «решай пошагово»;
- модель сначала сама составляет промпт для задачи, затем решает по нему;
- группа экспертов в промпте (аналитик, инженер, критик) — решение от каждого.
Сравнить: отличаются ли ответы, какой способ точнее.
**Результат:** несколько решений одной задачи и их сравнение.
**Формат:** Видео + Код.

### День 4. Температура
Выполнить один и тот же запрос с `temperature` = 0 / 0.7 / 1.2. Сравнить ответы по
точности, креативности, разнообразию. Сформулировать, для каких задач лучше каждая настройка.
**Результат:** примеры ответов с разной температурой и выводы.
**Формат:** Видео + Код.

---

## 6. Журнал по дням

> Дополняется по мере выполнения. Шаблон записи — ниже.

### День 1 — Первый запрос к LLM через API
- Статус: готово (код работает, ответ получен)
- Файлы: `week1/day1/day1.py`
- Что сделано: минимальный Python-скрипт (`requests`) — POST на `https://api.proxyapi.ru/openai/v1/chat/completions`, модель `gpt-4o-mini`, ключ из env `PROXYAPI_KEY`, печать ответа в консоль. Проверен запуск — API ответил.
- Примечание: при показе на Windows-консоли задать `$env:PYTHONIOENCODING="utf-8"` перед запуском, иначе кириллица в кракозябрах (кодировка терминала, не код).
- Ссылка на сдачу: —

### День 2 — Формат ответа
- Статус: готово (код работает, разница видна)
- Файлы: `week1/day2/day2.py`
- Что сделано: тот же вопрос («Назови три факта о Луне»), два запроса. (1) без ограничений — как День 1. (2) с контролем: явный формат (system: нумерованный список, по факту в строке), ограничение длины (`max_tokens: 100` + «≤10 слов»), условие завершения (`stop: ["КОНЕЦ"]` + инструкция дописать слово). Печатает оба ответа для сравнения. Проверен запуск — API ответил, контроль работает.
- Примечание: запуск с `$env:PYTHONIOENCODING="utf-8"` (как День 1).
- Ссылка на сдачу: —

### День 3 — Разные способы рассуждения
- Статус: готово (код работает, 4 разных ответа получены)
- Файлы: `week1/day3/day3.py`
- Задача: «Как попить воду из кружки, у которой нет дна, а верхушка запаяна?» (латеральная загадка).
- Что сделано: один `ask()`-хелпер, та же модель `gpt-4o-mini`. 4 способа: (1) прямой; (2) «решай пошагово»; (3) модель сама составляет промпт, затем второй вызов решает по нему (2 запроса); (4) группа экспертов в system (аналитик/инженер/критик) + строка «Итог:». Печатает все 4 для сравнения.
- Результат сравнения: ответы заметно отличаются по стилю. Прямой — слабейший («выпить невозможно»). Пошагово/самопромпт — креативнее (трубочка, наклон, просверлить), но переусложнено. Эксперты — самый структурный (3 взгляда + итог). Изящный ответ «перевернуть кружку» не нашёл ни один метод — хороший материал для видео.
- Примечание: `sys.stdout.reconfigure("utf-8")` уже в коде; на Windows-консоли при показе всё равно полезно `$env:PYTHONIOENCODING="utf-8"`.
- Ссылка на сдачу: —

### День 4 — Температура
- Статус: готово (код работает, 6 ответов получены: 3 температуры × 2 прогона)
- Файлы: `week1/day4/day4.py`
- Запрос: «Придумай название и слоган для баскетбольной команды и придумай тактику для неё, чтобы она всегда побеждала соперников.»
- Что сделано: один `ask(temperature)`-хелпер, модель `gpt-5.2`. Один и тот же запрос с `temperature` 0 / 0.7 / 1.2, по 2 прогона на каждую (RUNS=2) — чтобы было видно разнообразие на повторе. Печатает все 6 ответов сгруппированными по температуре.
- Проверка API: gpt-5.2 на ProxyAPI **поддерживает** кастомную temperature (0/0.7/1.2 проходят; значение >2 отклоняется ошибкой `decimal_above_max_value` — реальная валидация, не заглушка).
- Результат сравнения: точность высокая на всех температурах (фактических ошибок нет, везде честная оговорка «100% гарантии в спорте нет»). Креатив/разнообразие растут с температурой: temp=0 — суховато и шаблонно, 0.7 — баланс, 1.2 — макс лексики и экзотики терминов (прессинг 94 фута, 1-2-1-1, «энфорсер»), без мусора (gpt-5.2 держит связность даже на 1.2). Любопытно: **название «Стальные Ястребы» одинаковое во всех 6 прогонах** (сильный якорь модели), а слоган меняется даже при temp=0 — gpt-5.2 как reasoning-модель не полностью детерминирован при 0. Ядро тактики (5-out, пик-н-ролл, Spain PnR, switch, потери ≤10-12) везде одно, температура меняет обёртку.
- Выводы по применению: temp=0 — факты/код/классификация/повторяемость; temp=0.7 — ассистент, тексты, объяснения (баланс); temp=1.2 — брейншторм, варианты названий/идей.
- Примечание: `sys.stdout.reconfigure("utf-8")` в коде; на Windows-консоли при показе полезно `$env:PYTHONIOENCODING="utf-8"`.
- Ссылка на сдачу: —

<!--
Шаблон для нового дня:

### День N — <название задания>
- Статус: в работе / готово / сдано
- Файлы: <пути>
- Что сделано: <кратко>
- Ссылка на сдачу: <ссылка в таблице>
-->
