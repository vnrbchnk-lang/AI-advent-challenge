# CLAUDE.md — AI Advent Challenge #8

> Файл — постоянный контекст проекта. Claude Code читает автоматически при запуске
> из `C:\ADVENT`. Хранит контекст челленджа, инструменты, ограничения агента, журнал по дням.
> Журнал дополняется по мере выполнения — см. «Журнал по дням» внизу.

---

## 1. Что это за проект

Участие в **AI Advent Challenge #8** — 7-недельный марафон по LLM и ИИ-агентам
(организатор — курс mobiledeveloper.tech). Цель: выполнять ежедневные задания, оформлять, сдавать в срок.

- Сайт: https://mobiledeveloper.tech/ai_advent_8
- Сервисы (Notion): https://mobile-developer.notion.site/AI-Advent-3298aa43ca5580819b7ae6f5c90ee6f7
- Таблица сдачи: https://docs.google.com/spreadsheets/d/1GknoR-rJjllUj7WvgXFw-KxJJWwQOP9pYYji5Mx6uyM/edit?gid=0#gid=0

## 2. Правила челленджа (соблюдать)

- Задания выдают **по будням 14:00 МСК**.
- Выполнить **5 заданий/неделю**. Не сдал все 5 прошлой недели к понедельнику 14:00 МСК — **выбыл**.
- Сдача: заполнить слот в Google-таблице ссылками (режим комментирования).
  Организатор аппрувит. Нет результата — удаляют из таблицы/чата.
- **Формат каждого задания: Видео + Код.** (Видео участник пишет сам.)
- Проверка выборочная, асинхронная. Могут попросить переделать.
- Чат: вежливо, без мата/оскорблений/оффтопа.

## 3. Инструменты (стек проекта)

| Роль | Выбор | Детали |
|------|-------|--------|
| Harness (агент-помощник) | **Claude Code** | Сейчас Desktop, переходим в терминал. Работа из `C:\ADVENT`. |
| LLM API (для заданий) | **ProxyAPI.ru** | Российский посредник, оплата рублями с карты РФ. Один ключ → DeepSeek / OpenAI / Gemini / Anthropic. OpenAI-совместимый формат. |
| Язык/среда | **Python** | Минимальный код для REST. Модель `gpt-4o-mini` через OpenAI-эндпоинт ProxyAPI. |
| VPS/хостинг | (пока не надо) | Понадобится позже. Варианты: timeweb.com, beget.com/ru/vps, hosting-vds.com. |

### Harness ≠ API (не путать)
- **Claude Code (harness)** — пишет и запускает код. На подписке Claude.
- **ProxyAPI/DeepSeek (API из задания)** — облачная LLM, к ней *обращается код* по REST. Это и требуют задания («облачная модель через REST»).

### Доступ к ProxyAPI
- Регистрация и ключ: https://console.proxyapi.ru (нужен только email, без иностранного телефона).
- Оплата: рублями с карты любого банка РФ.
- Авторизация всех запросов: заголовок `Authorization: Bearer <КЛЮЧ>`.
- Базовый адрес: `https://api.proxyapi.ru`, далее идентификатор провайдера:
  - OpenAI (подтверждено докой): `https://api.proxyapi.ru/openai/v1` → `/chat/completions`
  - DeepSeek: в ProxyAPI **недоступен** (нет в консоли/доке на 2026-06). Не используем.

### Решение: провайдер и модель (зафиксировано 2026-06-03)
- Провайдер: **OpenAI** через ProxyAPI (OpenAI-совместимый формат).
- Эндпоинт: `https://api.proxyapi.ru/openai/v1/chat/completions`.
- Модель: **`gpt-4o-mini`** (подтверждена в списке: https://proxyapi.ru/docs/openai-models).
- Причина: задания минимальные → дёшево + быстро + качества хватает, без оверкилла.
- Ключ — в переменной окружения (`PROXYAPI_KEY`), НЕ коммитить в код/git.

---

## 4. ОГРАНИЧЕНИЯ ДЛЯ АГЕНТА (строго соблюдать)

Обязательны при работе над любым заданием:

1. **ТЗ выполнить полностью.** Учебный проект — пробовать необычное, выходить за рамки
   (доп. фичи, абстракции, эксперименты) полезно, если улучшает понимание или просто интересно.
   Но дословные требования дня закрыть обязательно. Добавляешь сверх ТЗ — явно говори, что этого не было в формулировке.
2. **Не выдумывать.** Задание не уточняет — спросить пользователя, не достраивать предположения. Не изобретать требований.
3. **Читаемость важнее объёма.** Код может быть сложнее «десятка строк», если оправдано учебной ценностью/фичей. Держать простым, без оверинжиниринга ради оверинжиниринга. Сложность — осознанно, не случайно.
4. **Точность фактов про API.** Не выдумывать эндпоинты, имена моделей, параметры. Не уверен — проверить в доке или спросить, не угадывать.
5. **Не выходить за рамки текущего дня.** Будущие дни заранее не начинать без явной просьбы.
6. **Формат сдачи.** Результат = Видео + Код. Код должен быть легко показуем и объясним на видео.
7. **Безопасность.** API-ключи только в переменных окружения, не в коде и не в репозитории.
8. **Сверяться с формулировкой.** Перед сдачей сверять с дословным ТЗ (см. `первые2днячелленджа.txt` и последующие).
9. **Журналировать.** После выполнения дополнять «Журнал по дням»: что сделано, какие файлы, ссылка на код, статус сдачи.
10. **Без комментариев в коде.** Без `#` и docstring. Код самодокументирующийся через понятные имена. Пояснения про задачу/выбор модели/цены — только в CLAUDE.md (журнал), не в `.py`.
11. **Не запускать сложных агентов самому (экономия токенов).** Агент (Claude Code) НЕ гоняет интерактивных/многошаговых агентов и сравнительные прогоны сам — жжёт токены ProxyAPI. Проверка агента — **ревью кода** (читаемость, логика, корректность вызовов API, верные имена моделей/эндпоинтов). **Запускает живьём пользователь** — сразу для видео. Исключение: одиночный быстрый smoke-вызов API, если пользователь явно попросил. Дорогие compare-скрипты — пишем, прогон за пользователем.
12. **Красивый CLI.** Рисовать приятный, читаемый интерфейс в стиле Claude Code: рамки/панели, цвета, аккуратные заголовки, таблицы, спиннер при запросе. Библиотека — `rich`. Интерфейс не мешает сути, но выглядит симпатично (идёт на видео).

---

## 5. Задания (формулировки)

Полные тексты первых двух дней — в `первые2днячелленджа.txt`.

### День 1. Первый запрос к LLM через API
Минимальный код, который:
- отправляет запрос в LLM через API;
- получает ответ;
- выводит в консоль или простой интерфейс (CLI / Web).
**Результат:** код, шлющий запрос в LLM через API и получающий ответ.
**Формат:** Видео + Код.

### День 2. Формат ответа
Тот же запрос, но:
- добавить явное описание формата ответа;
- добавить ограничение длины;
- добавить условие завершения (stop sequence или явная инструкция).
Сравнить: без ограничений vs с ограничениями.
**Результат:** один запрос с разным уровнем контроля ответа через API.
**Формат:** Видео + Код.

### День 3. Разные способы рассуждения
Одна задача (логическая/алгоритмическая/аналитическая), решить через API четырьмя способами:
- прямой ответ без доп. инструкций;
- инструкция «решай пошагово»;
- модель сама составляет промпт для задачи, затем решает по нему;
- группа экспертов в промпте (аналитик, инженер, критик) — решение от каждого.
Сравнить: отличаются ли ответы, какой способ точнее.
**Результат:** несколько решений одной задачи + сравнение.
**Формат:** Видео + Код.

### День 4. Температура
Один запрос с `temperature` = 0 / 0.7 / 1.2. Сравнить по точности, креативности, разнообразию. Сформулировать, для каких задач лучше каждая настройка.
**Результат:** примеры ответов с разной температурой + выводы.
**Формат:** Видео + Код.

### День 15. Контролируемые переходы состояний
Явные переходы между состояниями задачи: допустимые состояния; разрешённые переходы; ассистент не «перепрыгивает» этап (нет реализации до утверждённого плана; нет финала без валидации). Проверить: попытки перейти в недопустимое состояние, реакцию ассистента, корректность продолжения после паузы.
**Результат:** ассистент с контролируемым жизненным циклом задачи. **Формат:** Видео + Код / Текст.

---

## 6. Журнал по дням (вынесен в отдельные файлы)

Журнал разнесён по неделям, чтобы CLAUDE.md оставался лёгким:

- Неделя 1 (промптинг): **`JOURNAL-week1.md`** — дни 1–5.
- Неделя 2 (контекст-менеджмент, память): **`JOURNAL-week2.md`** — дни 6–10 + модель недели.
- Неделя 3 (память, состояние, stateful-агент): **`JOURNAL-week3.md`** — дни 11–15.
- Неделя 4 (MCP): **`JOURNAL-week4.md`** — дни 16–20.
- Неделя 5 (RAG): **`JOURNAL-week5.md`** — дни 21–25 (текущая).

Новые записи дня — в файл недели по шаблону:
```
### День N — <название>
- Статус: в работе / готово / сдано
- Файлы: <пути>
- Задание: <дословная формулировка ТЗ>
- Что сделано: <кратко>
- Сверх ТЗ: <что добавлено вне формулировки>
- Ссылка на сдачу: <ссылка в таблице>
```

---

## 7. Неделя 3 — память, состояние, stateful-агент (текущая)

> Контекст недели — конспект `03-memory-state-summary.md`. **Всегда сверяться по теории.**
> Темы: stateless→stateful, персонализация (профиль), memory layers, task state machine,
> инварианты, prompt builder, validation, save/resume. Цель недели — собрать всё в один **stateful-агент** к концу недели.

### Архитектурное решение недели 3 (зафиксировано 2026-06-18)
- **Единый растущий пакет, не 5 отдельных скриптов.** `week3/assistant/` — один пакет, каждый день добавляет одну абстракцию. К концу недели = собранный stateful-агент (цель недели по конспекту: «собрать части в управляемую систему»).
- Планируемая раскладка (уточняется по ходу, будущие дни заранее НЕ пишем):
  - `memory.py` — день 11 (memory layers): short-term / working / long-term.
  - `profile.py` — персонализация (стиль / constraints / контекст).
  - `state.py` — task state machine: `TaskStore` — **мультизадачный** (словарь именованных задач + указатель `current`), стадии + allowed transitions, статусы active/paused/done. **Переключение между задачами реализовано** (просьба пользователя): `/task <имя>` открывает рабочее пространство задачи (вложенный prompt-loop, стадии `/next`/`/back`, `/pause`, `/delete`); список задач со статусами всегда в нижней панели CLI (`bottom_toolbar`).
  - `invariants.py` — инварианты + проверка кодом (линтер требований).
  - `swarm.py` — день 15 (рой агентов): на **каждой** рабочей стадии свой рой из 5 ролей
    (`gpt-4o-mini`) + оркестратор (`gpt-4.1`) сводит мнения и возражает при конфликте с инвариантами.
    Роли по стадиям: planning (Архитектор/Безопасность/Производительность/Прагматик/Критик),
    execution (Бэкенд/Данные/Тесты/Интеграции/Критик кода), validation (Инварианты/Безопасность/
    Граничные случаи/Производительность/Критик); done — без роя, финальная сводка.
    Рой стартует **авто при входе на стадию** (создание задачи → planning; каждый `/next` →
    рой новой стадии). Итог planning = утверждённый план (связь со state machine: без
    плана нет execution); итоги остальных стадий пишутся в результаты задачи. **Стадию двигает
    только человек** командой `/next`/`/back` — LLM перехода не имеет (ядро дня 15). `/council` —
    ручной перезапуск роя текущей стадии.
  - `prompt_builder.py` — сборка prompt из выбранных слоёв.
  - `agent.py` — собранный stateful-агент (LLM engine + всё выше).
  - демо/сравнение каждого дня — командой в CLI (день 11 — `/demo`) либо отдельным скриптом; прогон за пользователем (правило 11).
- Хранилище раздельное по слоям: каталог `week3/assistant/store/` с отдельным JSON на слой (видно «хранится отдельно» — для видео нагляднее одного файла).
- Модель: **`gpt-4.1`** (сменили с gpt-5.2 ради экономии — выход 2062₽/1М против 4245₽; цена сверена на https://proxyapi.ru/pricing/list). Дешёвый экстрактор для будущих дней (извлечение профиля/фактов), если надо, — **`gpt-4o-mini`**. Эндпоинт и формат — те же (ProxyAPI OpenAI-совместимый).
- CLI — красивый, через `rich` + `prompt_toolkit` (слэш-команды с автодополнением); основной акцентный цвет — **тёмно-синий** (navy `#34568b`, константы `NAVY*`; сменён со стального серого на день 15 по просьбе пользователя), без эмодзи (правило 12).

### Задание дня 11 (дословно)
Описать и реализовать модель памяти ассистента. Разделить минимум на 3 типа: краткосрочная (текущий диалог), рабочая (данные текущей задачи), долговременная (профиль, решения, знания). Сделать: типы памяти хранятся отдельно; явно выбирается, что и куда сохраняется. Проверить: какие данные в каждый слой; как влияет на ответы.
**Результат:** агент с явной моделью памяти (memory layers). **Формат:** Видео + Код / Текст.

### Задания дней 12–14 (дословно) — реализованы вместе как собранный stateful-агент
По явной просьбе пользователя написан сразу полный агент (команда `agent11`, `assistant/cli.py`) со способностями дней 11–14; каждый день показан своей демо-командой по нарастанию: `/demo1` (день 11 — память), `/demo2` (день 12 — профиль), `/demo3` (день 13 — state machine), `/demo4` (день 14 — инварианты), `/demo5` (день 15 — контролируемые переходы). Профиль собирается интервью-командой `/interview`. День 15 добавил рой агентов (`/council`, см. `swarm.py`). Подробности — `JOURNAL-week3.md`.

**День 12. Персонализация.** Создать профиль пользователя; описать предпочтения (стиль, формат, ограничения); подключить профиль к каждому запросу. Проверить: ответы для разных профилей; что ассистент учитывает автоматически. **Результат:** персонализированный агент.

**День 13. Состояние задачи (Task State Machine).** Состояние задачи как конечный автомат: этап / текущий шаг / ожидаемое действие; пример planning→execution→validation→done. Проверить: паузу на любом этапе; продолжение без повторных объяснений. **Результат:** агент с формализованным состоянием задачи.

**День 14. Инварианты и ограничения состояния.** Добавить инварианты, которые агент не вправе нарушать (архитектура, тех-решения, стек, бизнес-правила). Сделать: инварианты хранятся отдельно от диалога; агент явно учитывает их в рассуждениях; отказывается от решений, нарушающих их. Проверить: что при конфликте запроса и инварианта; как объясняет отказ. **Результат:** ассистент в рамках инвариантов.

**День 15. Контролируемые переходы состояний.** Допустимые состояния + разрешённые переходы; ассистент не «перепрыгивает» этап (нет реализации без утверждённого плана, нет финала без валидации). Проверить: попытки недопустимого перехода, реакцию, продолжение после паузы. Реализация: ядро (`TRANSITIONS`, код-гейт `transition()`, плательный гейт execution, done терминальна) стояло с дня 13 — день 15 добавил демо `/demo5` (детерминированно показывает отказ кода на нелегальных переходах + пауза/резюм) и **рой агентов** `/council` (`swarm.py`, сверх дословного ТЗ — по просьбе оргкомитета: 5 ролей + оркестратор-арбитр). **Результат:** ассистент с контролируемым жизненным циклом задачи.

---

## 8. Неделя 4 — MCP (текущая)

> Контекст недели — конспект `04-mcp-summary.md`. Тема: MCP (host/client/server), подключение
> публичных tools, свой MCP-сервер вокруг API, оркестрация нескольких tools в агенте.

### Архитектурное решение недели 4 (зафиксировано 2026-06-26)
- **Единый растущий пакет `week4/agent/`, команда `agent16`** (`pip install -e week4` → console_script
  `agent16 = agent.cli:main`, по аналогии с `agent11`). Один MCP-сервер (FastMCP, stdio) + один
  агент-клиент; каждый день добавляет слой, функционал дней — командами внутри CLI.
- Раскладка: `cli.py` (host/CLI), `mcp_client.py` (коннект + list_tools), `mcp_agent.py` (постоянное
  соединение со своим сервером, call_tool, pipeline, LLM-loop), `server/app.py` (FastMCP),
  `server/wiki.py` (д.17), `server/scheduler.py` (д.18), `server/pipeline.py` (д.19), `store/` (рантайм).
- SDK — официальный Python `mcp` (1.28.0). Транспорты: Streamable HTTP (день 16, публичный DeepWiki),
  stdio (дни 17–19, свой локальный сервер).
- Async-мост: фоновый поток с asyncio-loop держит stdio-сессию своего сервера открытой; команды CLI шлют
  корутины через `run_coroutine_threadsafe` (один subprocess сервера живёт → scheduler дня 18 работает).
- Модель: `gpt-4.1` (основная), `gpt-4o-mini` (summarize, дёшево). ProxyAPI, тот же эндпоинт. CLI — navy
  `#34568b`, без эмодзи (правило 12). План недели — `~/.claude/plans/4-quiet-bubble.md`.

### Задание дня 16 (дословно)
Установить MCP SDK/клиент (или поднять MCP-сервер). Минимальный код: устанавливает MCP-соединение;
получает от MCP список доступных инструментов. Проверить: соединение устанавливается; список инструментов
корректно возвращается. **Результат:** код, подключающийся к MCP и выводящий список tools.

### Задание дня 17 (дословно)
Реализовать свой MCP-сервер вокруг любого API. Сделать: регистрацию инструмента; описание входных
параметров; возврат результата. Подключить инструмент к агенту: вызвать из приложения; получить и
использовать результат. **Результат:** агент делает вызов к MCP-инструменту и получает результат.

### Задание дня 18 (дословно)
MCP-инструмент с отложенным/периодическим выполнением (reminder, периодический сбор, регулярный summary).
Инструмент: сохраняет данные (JSON/SQLite); выполняется по расписанию; возвращает агрегированный результат.
**Результат:** агент, работающий 24/7 и периодически выдающий сводку.

### Задание дня 19 (дословно)
Несколько MCP-инструментов (search / summarize / saveToFile). Пайплайн: первый получает данные, второй
обрабатывает, третий сохраняет. Проверить: автоматическое выполнение цепочки; корректность передачи данных
между инструментами. **Результат:** автоматический пайплайн из нескольких MCP-инструментов.

### Задание дня 20 (дословно)
Зарегистрировать несколько MCP-серверов. Сделать: агент выбирает нужный инструмент; корректно маршрутизирует
запросы; выполняет длинный флоу. Проверить: сценарий с инструментами с разных серверов; корректность выбора
и порядка вызовов. **Результат:** длинный флоу взаимодействия с несколькими MCP-серверами и инструментами.

### Раскладка дня 20 (зафиксировано 2026-06-26)
- Монолит `server/app.py` разбит на **4 зарегистрированных сервера**: 3 свои stdio
  (`wiki_app`/`scheduler_app`/`pipeline_app`) + публичный DeepWiki по HTTP. Модули реализации tools
  (`wiki.py`/`scheduler.py`/`pipeline.py`) не тронуты. `app.py` удалён.
- `mcp_agent.py` — мульти-серверный реестр `SERVERS`: одна фон-корутина держит ВСЕ сессии открытыми
  (`AsyncExitStack`), каждый сервер коннектится отказоустойчиво (try/except + `wait_for`). Tools
  неймспейсятся `server__tool`; роутер `call_tool(server, name, args)`. Два детерминированных
  кросс-серверных флоу (`run_flow`, `run_research`) + LLM-режим `ask` (выбор tools всех серверов).
- CLI-команды: `/servers`, `/tools` (по серверам), `/flow`, `/research`, `/ask`, `/demo20`, `/demo16`.

---

## 9. Неделя 5 — RAG (текущая)

> Контекст недели — конспект `5_неделя_6_поток_v1.md`. Тема: эмбеддинги, chunking, векторный
> поиск, реранкинг, цитаты/анти-галлюцинации, RAG-чат с памятью. Журнал — `JOURNAL-week5.md`.

### Архитектурное решение недели 5 (зафиксировано 2026-07-04)
- **Единый пакет `week5/rag/`, команда `agent21`** (`pip install -e week5`). Дни 21–25 — команды
  одного CLI: `/fetch` `/index` `/search` `/demo21` (д.21), `/ask` `/raw` `/eval` (д.22),
  `/mode` `/compare23` (д.23), `/check` (д.24, цитаты + «не знаю»), `/chat` (д.25) + `/demo22..25`.
- **Эмбеддинги — Ollama локально** (`bge-m3`, 1024-dim, мультиязычная; `localhost:11434/api/embed`).
  Выбор пользователя: privacy (база не покидает машину) + бесплатно. Установку и прогоны агент
  делает сам (указание пользователя 2026-07-04, см. memory `do-setup-yourself`).
- **Корпус — база хакатона `C:\Alaba`** («Личная культура»): `.memory-bank/*.md` + `raw/*.txt` +
  `raw/USM.pdf` (pypdf) + README = 20 доков, ~201К символов. **В git не коммитится** (данные
  заказчика): `rag/docs/`, `rag/store/` в .gitignore, сборка читает `C:\Alaba` напрямую.
- **Индекс — JSON + numpy**: нормализованные векторы, косинус = dot, брутфорс (~160 чанков).
- Чанкинг, 2 стратегии: **fixed** (1600 симв., overlap 200, по предложениям) vs **structural**
  (по заголовкам md, `section` = путь заголовков). Метаданные: `chunk_id/strategy/source/group/
  title/section/n_chars`.
- Конвейер поиска (д.23): rewrite (gpt-4o-mini, опц.) → topN=20 → порог косинуса 0.42
  (подобран по скорам: легитимные вопросы 0.44–0.65, оффтоп ≤0.38 на bge-m3) →
  LLM-реранк (gpt-4o-mini 0–10, отсечка <4, опц.) → topK=5.
- Анти-галлюцинации (д.24): ответ строго JSON `{answer, sources, quotes}`; цитаты валидируются
  кодом (подстрока чанка); «не знаю» — код-гейт ДО вызова LLM, если контекст слабый.
- Чат (д.25): история (окно 8) + память задачи `{goal, clarified, constraints, glossary}`
  (экстрактор gpt-4o-mini, `store/chat-*.json`, resume); rewrite вопроса с учётом истории.
- Генерация — ProxyAPI: `gpt-4.1` (ответы), `gpt-4o-mini` (вспомогательное). CLI — navy, без эмодзи.